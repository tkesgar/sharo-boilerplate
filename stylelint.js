/**
 * This is a stylelint configuration available for use.
 *
 * This configuration uses `scss` plugin and use `recommended` and
 * `recommended-scss` rules.
 */
module.exports = {
  plugins: [
    'stylelint-scss'
  ],
  extends: [
    'stylelint-config-recommended',
    'stylelint-config-recommended-scss'
  ]
}
